import Button from "../Button";

import "./Main.css"

const Main = () => {
  return (
    <>
      <main>
        Main
        <Button onClick={() => alert("text")}>Text</Button>
      </main>
    </>
  )
}

export default Main
